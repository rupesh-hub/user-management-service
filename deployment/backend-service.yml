appVersion: v1
kind: Service
metadata:
  name: user-management-service
  labels:
    app: user-management-service

spec:
  type: ClusterIP
  ports:
    - port: 8181
      targetPort: 8181
      protocol: TCP
      name: http
  selector:
    app: user-management-service